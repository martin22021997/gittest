Projekt1:

connector auttest1(){
	int a;
	int b;
	int c;
	
	BASIC sub;
	
	BASIC pow;
	
	sub(a,b+c) : result;
	
	pow(result) : powered;
}

basic sub( int a, int b ) : int result {
	//change1
	result = 400*a - 400*b;
}

basic pow( int num ) : int powered {
	powered = num * num;
}